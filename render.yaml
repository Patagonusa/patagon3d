services:
  - type: web
    name: patagon3d
    runtime: python
    buildCommand: pip install -r requirements.txt
    startCommand: python run.py
    envVars:
      - key: LUMA_API_KEY
        sync: false
      - key: PORT
        value: 10000
    healthCheckPath: /api/health
